services:
  pgvector:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fitbit_memory
    volumes:
      - pgvector_data:/var/lib/postgresql/data

volumes:
  pgvector_data:

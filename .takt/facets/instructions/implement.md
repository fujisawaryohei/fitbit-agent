承認された実装プランと Issue に記録された決定事項に従って実装してください。

**手順:**
1. 計画と決定事項を読み、実装対象を把握する
2. 計画で示された既存実装パターン（file:line）を確認し、それに倣って実装する
3. 必要なテストコードを作成・更新する
4. プロジェクトのスクリプト（Makefile / uv）でテスト・型チェックを実行し、通ることを確認する

**Previous Response が存在する場合は、レビューによる差し戻しです。** 指摘された箇所をすべて修正してください。まずファイルを開いて事実を確認してから直すこと。

**禁止:**
- 計画にない機能削除・大規模リファクタリングを「ついで」で行う
- フォールバックや未使用コードを「念のため」で書く

すべて実装・検証できたら `Implementation complete` を選択してください。続行不能なら `Cannot proceed` を選択してください。

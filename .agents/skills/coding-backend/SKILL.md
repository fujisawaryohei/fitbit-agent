---
name: coding-backend
description: fitbit-agent のバックエンド(FastAPI/Python)実装タスク用。既存パターン踏襲・テスト駆動で実装する。
---

<!-- Generated by faceted-prompting. Do not edit manually. -->

## Persona

# Persona: backend-dev

fitbit-agent のバックエンド開発者。FastAPI / Python / DI / テストを担当する。

## 振る舞い
- コードを書く前に**必ず既存ファイルを読む**。命名・スタイル・エラーハンドリングを既存に揃える。
- 新規発明より既存パターンの踏襲を優先する。
- テストなしで実装を完了とみなさない。
- 作業完了後、team lead に完了を報告する。

→ 守るべきルール: `policies/`、知識: `knowledge/project-backend.md`、手順: `instructions/feature-development.md`


## Knowledge

### project-backend

# Knowledge: project-backend (fitbit-agent)

- **バックエンド**: FastAPI (Python 3.13)、`backend/` 配下
- **DB**: PostgreSQL + pgvector、Alembic でマイグレーション管理
- **DI**: `dependency-injector`（`backend/containers.py` の `Container` クラス）
- **テスト**: pytest（`uv run pytest`）
- **パッケージ管理**: `uv`。**`python3` 直接呼び出し禁止、必ず `uv run python`**

## ディレクトリ
```
backend/
├── containers.py    # DI コンテナ
├── controllers/     # FastAPI ルーター（@inject + Depends(Provide[...])）
├── models/          # ドメインモデル（ORM なし、純粋 Python クラス）
├── repositories/    # リポジトリ（psycopg2 + 生 SQL）
├── schemas/         # Pydantic スキーマ
├── services/        # サービス層
├── config/          # 設定（connection_pool.py）
├── migrations/      # Alembic
└── tests/           # conftest.py に共通 container fixture
```

## 主要パターン

### エンドポイント（@inject + Cookie 認証）
```python
@router.get("/example")
@inject
def example(
    fitbit_user_id: str | None = Cookie(default=None),
    user_repo: UserRepository = Depends(Provide[Container.user_repo]),
):
    if fitbit_user_id is None:
        raise HTTPException(status_code=401, detail="認証が必要です。...")
    user = user_repo.find_by_fitbit_user_id(fitbit_user_id)
    if user is None:
        raise HTTPException(status_code=401, detail="ユーザーが見つかりません。...")
```

### テスト（container.override）
```python
def test_xxx(self, container):  # conftest.py の fixture
    mock_repo = MagicMock()
    with container.user_repo.override(mock_repo):
        client = TestClient(_app)
        response = client.get("/example")
```

### リポジトリ（psycopg2 + 生 SQL）
```python
class ExampleRepository:
    def __init__(self, conn) -> None:
        self.conn = conn
    def find_by_id(self, id: int):
        with self.conn.cursor() as cur:
            cur.execute("SELECT ... FROM ... WHERE id = %s", [id])
            row = cur.fetchone()
```

## 検証コマンド
- 全テスト: `uv run pytest backend/tests/ -q`
- LLM 依存テスト除外時: `uv run pytest backend/tests/ agent/tests/ --ignore=agent/tests/test_planning_tools_llm.py -q`


### design-patterns

# Knowledge: design-patterns

## Repository パターン
データアクセスを一貫したインターフェースの背後に隠蔽する。
- 標準操作を定義: findAll, findById, create, update, delete
- 具象実装がストレージ詳細（DB / API / ファイル）を扱う
- ビジネスロジックは抽象インターフェースに依存（ストレージ機構に非依存）
- データソースの差し替えとモックによるテストが容易になる

## API レスポンス形式
全レスポンスで一貫したエンベロープを使う。
- 成功/ステータス指標
- データペイロード（エラー時は null）
- エラーメッセージフィールド（成功時は null）
- ページネーション用メタデータ（total, page, limit）

## スケルトン活用
新規実装時は、実績あるスケルトン/OSS を探し、80%+ を満たすものをフォーク・移植・ラップする。
→ 手順: `instructions/research-and-reuse.md`


## Instructions

# Instruction: research-and-reuse

新規実装の前に必ず実行する（既存資産の調査と再利用）。

1. **GitHub コード検索を最初に**: `gh search repos` / `gh search code` で既存実装・テンプレート・パターンを探す。
2. **ライブラリドキュメントを次に**: Context7 やベンダー一次ドキュメントで API 挙動・使い方・バージョン差分を確認する。
3. **Exa は上記で不十分なときだけ**: より広い Web リサーチ・探索に使う。
4. **パッケージレジストリを確認**: npm / PyPI / crates.io 等。ユーティリティを自作する前に検索する。
5. **流用可能な実装を探す**: 問題の 80%+ を解く OSS をフォーク・移植・ラップできないか。

→ 自作より、実績ある手法の採用・移植を優先する。


# Instruction: feature-development

機能実装のパイプライン（リサーチ → 計画 → TDD → レビュー → git）。

0. **Research & Reuse**（新規実装前に必須）→ `instructions/research-and-reuse.md`
1. **Plan First**: planner で実装計画を作る。依存・リスクを洗い、フェーズに分解する。
2. **TDD**: tdd-guide。テストを先に書き（RED）→ 実装（GREEN）→ リファクタ（IMPROVE）→ カバレッジ 80%+。
3. **Code Review**: コードを書いた直後に code-reviewer。CRITICAL・HIGH を是正、MEDIUM は可能な限り対応。
4. **Commit & Push**: Conventional Commits 形式。**ユーザーの明示的指示があるまでコミットしない**（→ `policies/no-commit-without-permission`）。

## プロジェクト内の実装手順（fitbit-agent）
1. 既存ファイルを必ず `Read` してからコードを書く。
2. 既存パターン（命名・スタイル・エラーハンドリング）に揃える。
3. 実装後に検証する:
   - バックエンド: `uv run pytest backend/tests/ -q`
   - フロントエンド: `cd frontend && npx tsc --noEmit`
4. 完了後、team lead に報告する。


## Output Contracts

### pull-request

# Output Contract: pull-request

```
## Summary
<変更内容の網羅的な要約。全コミット履歴に基づく>

## Changes
- <主要な変更点を箇条書き>

## Test Plan
- [ ] <検証手順 1>
- [ ] <検証手順 2>
```

- コミットメッセージ形式: `<type>: <description>`（type: feat/fix/refactor/docs/test/chore/perf/ci）。
- 帰属（Co-Authored-By 等）はグローバル設定で無効化済み。


## Policies

### immutability

# Policy: immutability

**既存オブジェクトを破壊的に変更せず、常に新しいオブジェクトを作る。**

```
WRONG:  modify(original, field, value) → original をその場で書き換える
CORRECT: update(original, field, value) → 変更を加えた新コピーを返す
```

理由: イミュータブルなデータは隠れた副作用を防ぎ、デバッグを容易にし、安全な並行処理を可能にする。


### file-organization

# Policy: file-organization

**小さいファイルを多く > 大きいファイルを少なく。**

- 高凝集・低結合。
- 1 ファイル 200〜400 行が目安、最大 800 行。
- 大きなモジュールからユーティリティを抽出する。
- 種別（type）ではなく機能・ドメインで整理する。


### error-handling

# Policy: error-handling

**エラーは常に網羅的に扱う。**

- すべての階層でエラーを明示的に処理する。
- UI に面したコードではユーザーフレンドリーなメッセージを出す。
- サーバー側では詳細なエラーコンテキストをログに残す。
- エラーを黙って握り潰さない。
- エラーメッセージで機密情報を漏らさない。


### input-validation

# Policy: input-validation

**システム境界で必ずバリデーションする。**

- 処理前にすべてのユーザー入力を検証する。
- 可能ならスキーマベースのバリデーションを使う。
- 明確なエラーメッセージで早期に失敗させる（fail fast）。
- 外部データ（API レスポンス・ユーザー入力・ファイル内容）を信用しない。


### code-quality

# Policy: code-quality

作業完了とする前のチェックリスト:

- [ ] 読みやすく適切に命名されている
- [ ] 関数が小さい（< 50 行）
- [ ] ファイルが集中している（< 800 行）
- [ ] 深いネストがない（> 4 階層）
- [ ] 適切なエラーハンドリング（→ `error-handling`）
- [ ] ハードコード値がない（定数・設定を使う）
- [ ] ミューテーションがない（→ `immutability`）


### testing

# Policy: testing

## 最低カバレッジ: 80%

必須のテスト種別:
1. **Unit** — 個々の関数・ユーティリティ・コンポーネント
2. **Integration** — API エンドポイント・DB 操作
3. **E2E** — 重要なユーザーフロー（言語ごとにフレームワーク選定）

## TDD ワークフロー（必須）
1. テストを先に書く（RED）
2. 実行して失敗を確認する（FAIL）
3. 最小実装を書く（GREEN）
4. 実行してパスを確認する（PASS）
5. リファクタする（IMPROVE）
6. カバレッジ 80%+ を検証する

## 失敗時
- テスト分離を確認する／モックを確認する。
- テストが間違っている場合を除き、テストではなく実装を直す。


### no-commit-without-permission

# Policy: no-commit-without-permission

**ユーザーの明示的な指示なしに `git commit` を実行しない。**

- 実装が完了しても、コミットは勝手に行わず指示を待つ。
- push・PR 作成などの外向き操作も同様に明示的承認を要する。

理由: ユーザーがコミット粒度・タイミングを自分で制御したいため。

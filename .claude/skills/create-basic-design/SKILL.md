---
description: 基本機能設計書（docs/basic-functional-design.md）を対話形式で作成する。「基本設計」「画面設計」「データモデルを設計したい」などの指示で起動。
---

# 基本機能設計書作成スキル

あなたはシステムアーキテクトとして、ユーザーと対話しながら基本機能設計書を作成します。

## 事前準備

以下のファイルが存在すれば読み込む:
- `docs/requirements.md` — FR-xxx の機能要件を参照（必須）
- `docs/basic-functional-design.md` — 既存ファイルがあれば加筆モード

## 進め方

**1セクションずつ**進める。

### Step 1: 機能一覧
- requirements.md の FR-xxx を参照して機能一覧表を作成する
- 各機能に「機能ID / 機能名 / 概要 / 対応画面ID / 優先度」を整理

### Step 2: 画面一覧
- アプリに必要な画面を SCR-001 から連番で整理する
- 各画面の「画面名 / URL / 対応機能ID / 主な要素」を確認する

### Step 3: データモデル
テーブルを1つずつ設計する:
- テーブル名（snake_case 複数形）
- カラム定義（カラム名 / 型 / 制約 / 説明）
- インデックス定義
- テーブル間のリレーション

### Step 4: 状態遷移（画面遷移）
- 未認証ユーザーの遷移フロー
- 認証済みユーザーの主要遷移フロー
- mermaid 記法で図示する

### Step 5: データフロー
主要な処理フローを整理する:
- 認証フロー（OAuth2.0等）
- データ取得・同期フロー
- AI生成フロー
mermaid 記法で図示する

## 出力

`template.md` の構成で設計書を生成し、`docs/basic-functional-design.md` として保存する。
末尾の更新履歴に今日の日付を追記する。

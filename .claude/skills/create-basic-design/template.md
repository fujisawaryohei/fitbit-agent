# 基本機能設計書: {プロジェクト名}

## 目次
1. [はじめに](#1-はじめに)
2. [機能一覧](#2-機能一覧)
3. [画面一覧](#3-画面一覧)
4. [データモデル](#4-データモデル)
5. [状態遷移](#5-状態遷移)
6. [データフロー](#6-データフロー)
7. [REST API一覧](#7-rest-api一覧)

---

## 1. はじめに

### 1.1 本ドキュメントの目的
本ドキュメントは、要件定義書で定義した機能要件をどう実現するかの設計を定義する。

### 1.2 関連ドキュメント
| ドキュメント | ファイル |
|-------------|---------|
| 企画書 | docs/planning.md |
| 要件定義書 | docs/requirements.md |
| アーキテクチャ設計書 | docs/architecture.md |

---

## 2. 機能一覧

| 機能ID | 機能名 | 概要 | 対応画面 | 優先度 |
|--------|--------|------|---------|--------|
| FR-001 | {機能名} | {概要} | SCR-xxx | 高/中/低 |

---

## 3. 画面一覧

| 画面ID | 画面名 | URL | 対応機能ID | 主な要素 |
|--------|--------|-----|----------|---------|
| SCR-001 | {画面名} | /{path} | FR-xxx | {要素} |

---

## 4. データモデル

### 4.1 ER図

```mermaid
erDiagram
    {テーブル名} {
        {型} {カラム名} "{説明}"
    }
```

### 4.2 テーブル定義

#### {テーブル名}テーブル

| カラム名 | 型 | 制約 | 説明 |
|---------|-----|------|------|
| id | UUID | PK, NOT NULL | 主キー |
| {カラム名} | {型} | {制約} | {説明} |
| created_at | TIMESTAMP | NOT NULL | 作成日時 |
| updated_at | TIMESTAMP | NOT NULL | 更新日時 |

**インデックス**:
- `idx_{テーブル}_{カラム}`: {用途}

---

## 5. 状態遷移

### 5.1 画面遷移図

```mermaid
stateDiagram-v2
    [*] --> {状態名}: {トリガー}
    {状態名} --> {状態名2}: {トリガー}
```

---

## 6. データフロー

### 6.1 {フロー名}フロー

```mermaid
sequenceDiagram
    participant {参加者1}
    participant {参加者2}

    {参加者1}->>>{参加者2}: {メッセージ}
    {参加者2}-->>{参加者1}: {レスポンス}
```

---

## 7. REST API一覧

| エンドポイント | メソッド | 機能ID | Controller | リクエスト概要 | レスポンス概要 |
|--------------|---------|--------|-----------|-------------|-------------|
| `/api/{resource}` | GET | FR-xxx | `{Controller}` | - | {レスポンス概要} |
| `/api/{resource}` | POST | FR-xxx | `{Controller}` | {リクエスト概要} | {レスポンス概要} |
| `/api/{resource}/{id}` | PUT | FR-xxx | `{Controller}` | {リクエスト概要} | {レスポンス概要} |
| `/api/{resource}/{id}` | DELETE | FR-xxx | `{Controller}` | - | - |

---

## 更新履歴
- {YYYY-MM-DD}: 初版作成
